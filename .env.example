NUXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# Keep "false" until there is enough content to open the site to Google.
# Set to "true" to allow crawling/indexing (enables robots.txt Allow + removes noindex meta).
NUXT_PUBLIC_ALLOW_INDEXING=false
